import React from 'react';

const sampleInput = (props:any) => {
return(
    <div>
       <input className={props.inputClass} placeholder={props.inputPlaceHolder}>
         
          </input>
    </div>
    )
}

export default sampleInput;